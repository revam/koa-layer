// from packages
import * as koa from 'koa';
// from library
import { Layer } from '../src';
import { waterfall } from './helpers';

describe('Layer', () => {
  it('should produse some result', async(done) => {
    done();
  });
});
